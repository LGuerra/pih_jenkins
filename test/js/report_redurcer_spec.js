import { expect } from 'chai';

console.log('AQUI')

import {
  onSetViviendaInfo
} from '../../client/scripts/actions/report_actions';

/*
describe('actions', () => {
  it ('should create an action to change vivienda info', () => {
    const info = {
      info: 'info'
    };
    const expectedAction = {
      type: FETCH_COLONIA_INFO,
      payload: info
    };

    expect(onSetViviendaInfo(info)).toEqual(expectedAction);
  });
});
*/